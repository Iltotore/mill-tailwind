//| mvnDeps: ["io.github.iltotore::mill-tailwind::0.1.1-2-541a1c-DIRTYbca2bf1c-SNAPSHOT"]

import mill._, scalalib._,scalajslib._
import io.github.iltotore.tailwind.TailwindModule

object `package` extends ScalaJSModule with TailwindModule:

  def scalaVersion = "3.7.1"

  def scalaJSVersion = "1.19.0"

  def tailwindVersion = "4.1.8"

  def mvnDeps = Seq(mvn"io.indigoengine::tyrian-io::0.14.0")

  override def moduleKind = Task(mill.scalajslib.api.ModuleKind.ESModule)